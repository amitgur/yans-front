export function setAuthChecked(state, status) {
  state.authChecked = status;
}

export function setIsSignIn(state, status) {
  state.isSignIn = status;
}

export function setUser(state, user) {
  state.user = user;
}

export function setLanguage(state, payload) {
  state.language = payload;
}
